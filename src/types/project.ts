export interface CreateProjectInterface {
  title: string;
  description: string;
  totalSpent: number;
}

export interface ProjectInterface {
  _id: string;
  title: string;
  description: string;
  totalSpent: number;
}
